# HotCopy 创作资产与 Premium 爆款雷达：实施计划（供 Antigravity 审核）

**文档用途：** 先审核产品范围、采集可行性和隔离方案；审核通过后再另行开始实现。本文是计划，不表示功能已经开发或上线。

## 1. 产品目标

在 HotCopy 当前产品内形成“发现选题 + 生成可发布内容”的订阅价值：Pro/Premium 用户可从现有素材生成小红书图文、定向口播脚本和知识脑图；Premium 再增加 YouTube 头部频道监控与后续限定来源的黑马发现。

用户始终在 HotCopy 同一个账号、同一套页面中使用这些功能。点击雷达视频后，用户可进入现有处理流程取得原声稿、摘要、翻译或五平台口播文案，再按需生成新的成套内容资产。

## 2. 必须遵守的代码边界

### 冻结的核心

不得修改或重构以下现有核心：

- `backend/server.js` 中的 `POST /api/generate`、平台路由、字幕与音频提取、Whisper 转录、LLM 降级、流式响应、`copies_cache` 读写和额度扣减。
- 现有支付回调、会员套餐和用户认证逻辑。
- 现有音频上传与清理流程。

雷达需要调用现有处理能力时，只通过已经存在的 HotCopy 页面交互或现有生成接口传入视频 URL。不得把雷达的采集、轮询或黑马逻辑塞入核心处理函数。

### 可新增的外围代码

- 在现有 HotCopy 页面增加导航入口和雷达界面。用户看到的是同一个 HotCopy 产品，不增加第二个用户应用。
- 新增隔离目录 `backend/addons/`，承载内容资产 API、雷达 API、采集适配器和定时任务。
- 附加服务使用独立 SQLite 文件，不得给核心 `database.sqlite` 加资产或雷达表。
- 附加 API 与雷达 Worker 由独立 PM2 进程运行。附加服务崩溃或暂停时，现有 HotCopy 转录、改写和支付服务继续工作。
- 新的反向代理路径应只转发到附加服务；不得改写现有 `/api/generate` 路由。

## 3. 当前代码事实

以下是计划编写时对现有代码的核对结果：

- `backend/server.js` 的 YouTube 转换优先调用 `youtube-transcript`，字幕不可用时调用 `yt-dlp` 提取音频，再送 Whisper 转录。此转换主链没有调用 YouTube Data API。
- B 站转换会读取移动端视频页面和播放器字幕信息；没有字幕时提取播放流并转录。
- TikTok 转换会通过第三方解析服务取得音频，再转录。
- 小宇宙与 Apple Podcasts 先尝试抓取公开页面中的音频地址；Apple 另有 iTunes Lookup 回退。
- 当前已有 `GET /api/auth/me`，能返回账号邮箱、套餐、用量和到期时间，可供新雷达服务校验现有登录用户与 Premium 权限。
- 当前热点榜单 `updateTrendsJob()` 与核心转换链是独立功能。榜单中的 YouTube Data API 是可选来源，不能据此认为主转换链依赖该 API。
- `copies_cache` 是按 `(video_id, mode)` 复用结果的全局缓存，并非用户作品库。雷达收件箱需要使用独立的用户关联数据。
- 当前没有博主监控、流量快照、黑马识别或雷达收件箱实现。
- 当前套餐的公开页面：Basic `$4.9` 提供每月 50 次现有整理模式，页面写有“开放全部整理模式”；Pro `$9.9` 提供每月 100 次并开放现有“AI 爆款改写”；Premium `$19.9` 提供每月 300 次、全部模型权限和优先处理。现有 Pro 改写已包含抖音、小红书、视频号、TikTok、YouTube 五个平台口播初稿。
- 核心 `POST /api/generate` 当前只接受 `raw`、`summary`、`translate`、`rewrite` 四种模式；新内容资产若要保持核心冻结，不能把生成类型、提示词或新的核心额度逻辑塞进该接口。

代码定位：YouTube 音频回退在 `backend/server.js` 的 `fetchYouTubeWhisperTranscript()`；播客页面抓取在 `fetchPodcastAudio()`；B 站与 TikTok 适配器分别为 `fetchBilibiliTranscript()`、`fetchTikTokTranscript()`；核心入口为 `POST /api/generate`；账号核验入口为 `GET /api/auth/me`；现有首页 Fetch 流式读取位于 `frontend/index.html` 的 `runConvertDirect()`。

## 4. 内容资产生成：订阅增值与留存功能

雷达负责“发现值得做的选题”；内容资产工作台负责“把素材整理成可发布的成品”。两者作为 HotCopy 同一账号、同一站内工作流的附加值。生成结果先让用户预览、编辑和复制；未经用户操作，不自动发布到任何平台。

### 三个新增动作

在已有结果页增加三个清晰的生成入口，视觉沿用 HotCopy 当前品牌和手机端布局，不另起一个产品：

1. **生成小红书爆款图文**：基于原始字幕或已核实的全文，生成痛点引入、3 个有信息量的正文段落、适量 Emoji 分段和 5 个相关 Tag。标题、正文、Tag 分区展示，支持编辑、复制和 Markdown/TXT 导出。不得编造原素材没有支持的事实、经历或结论。
2. **生成短视频口播脚本**：由用户选择抖音、小红书、视频号、TikTok 或 YouTube，按平台分别生成【黄金前 3 秒 Hook】【痛点共鸣】【干货反转】【引导关注】。控制口语节奏，输出可直接录制的稿件；Hook 与事实结论必须能从素材中追溯，关注引导可编辑且不强行添加夸张承诺。
3. **生成长文知识脑图**：从长视频内容提炼主题、主分支、子节点与证据要点。服务端优先返回有校验结构的 JSON 树，前端渲染为可读卡片/树图，并支持 Markdown、Mermaid 和 TXT 下载。Mermaid 仅作为可导出表示；渲染时按安全规则处理，禁止把未净化内容直接注入 HTML。

当前五平台口播改写属于核心现有权益，不应在宣传中假装是新功能。新增口播入口的价值应体现为：按单个平台定向生成、固定四段脚本结构、可编辑成稿；原有“AI 爆款改写”保留原有行为和套餐承诺。

### 建议的套餐权限（不削减已售权益）

| 套餐 | 保留的现有权益 | 新增内容资产建议 |
| --- | --- | --- |
| 免费 | 保持当前 3 次体验和当前页面已有能力，避免计划未经审核就把用户锁定为“仅原字幕” | 不开放新增资产生成，显示成品样例和升级入口 |
| Basic `$4.9` | 保持每月 50 次及当前页面承诺的全部现有整理模式、双语精翻 | 不开放新增资产生成；套餐原权益不降级 |
| Pro `$9.9` | 保持每月 100 次及现有 AI 爆款改写 | 开放小红书图文与平台定向短视频口播；脑图可开放限量次数，作为 Pro 升级理由之一 |
| Premium `$19.9` | 保持每月 300 次、全部模型和优先处理 | 开放三个资产的较高月度额度、作品保存/导出权益及爆款雷达 |

产品草案中“免费仅原字幕、Basic 双语精翻、口播与脑图限 Pro/Premium”的设想，与当前页面的 Basic“开放全部整理模式”承诺及 Free 当前可用模式存在冲突。审核时应先决定是否更新套餐销售文案、存量用户权益和服务端 entitlement；实施计划不得静默降低已售套餐权益。上述建议矩阵保留现有承诺，并把新增的付费价值放在新增资产、额度和雷达上。脑图若要成为 Pro 转化点，可在 Pro 提供较小月额度，Premium 提供更高额度。

### 不改核心的生成链路

1. 用户在结果页选择资产类型和目标平台。前端复用已有转换结果中的原始字幕；若结果页尚无原文，沿用现有 URL 输入和转换交互获取 `raw` 内容。
2. 前端将必要的来源标识、用户选择和原文发送到新增的 HotCopy 附加服务；浏览器不持有大模型 API Key。附加服务对输入长度、来源类型、用户套餐和频率做校验。
3. 附加服务调用服务端配置的大模型，使用独立的资产生成提示词、固定输出结构和响应长度上限；模型错误时返回可理解的失败状态，不触发核心模型降级链，也不改变核心缓存。
4. 前端按类型渲染可编辑结果，提供复制、Markdown/TXT/Mermaid 下载。用户明确点击“保存”后才写入个人资产库；默认不永久保存完整字幕或上传媒体。

用户身份通过附加服务转发请求至现有 `/api/auth/me` 做套餐校验；不共享 `JWT_SECRET`，不保存 Token。资产生成使用独立的月度额度台账，和核心 `used_count` 分开显示、分开扣减，避免未改核心却伪称共享核心额度。最终额度数值须结合模型成本和月度毛利目标确定，并由 Antigravity 审核。

### 附加服务建议结构

为控制 1G VPS 的常驻进程和部署面，内容资产 API 与雷达 API 可放在同一个隔离的附加服务中，代码按模块分开；雷达采集 Worker 仍独立进程运行。核心 `backend/server.js`、核心数据库和既有生成行为继续冻结。

```text
backend/addons/
  server.js                 # 附加 API、登录套餐校验、内容资产额度
  worker.js                 # 雷达轮询任务，与 API 进程隔离
  assets/                   # 图文、定向口播、脑图生成与输出校验
  radar/                    # YouTube 来源适配器、频道与异动逻辑
  db.js                     # 附加服务自己的参数化 SQLite 查询
  README.md                 # 启动、监控、停止与回滚
```

新增服务只通过独立代理路径暴露，例如 `/api/addons/*`，不可接管或改写 `/api/generate`。资产记录、月度用量和雷达数据存放在附加服务自己的 SQLite 文件；SQL 使用预编译参数。密钥由部署环境注入，不写入源码、前端或日志，也不在交接过程中读取或输出 `.env` 内容。不同套餐端点均须服务端校验，不能只靠前端隐藏按钮。

## 5. 爆款雷达与附加服务分阶段交付

### 阶段 0：来源验证与频道名单审核

先验证采集适配器，再写定时任务：

1. 选取少量已人工核实的公开频道，确认频道 ID、频道名称与赛道对应关系。
2. 验证频道公开 Feed 是否稳定返回新视频 ID、标题、发布时间和链接。
3. 单独验证现有 `yt-dlp` 环境或其他网页解析器能否稳定读取公开视频的播放量、发布时间、频道信息。现有 `yt-dlp` 在核心流程中只用于音频提取，不能假设它已经能满足雷达的统计需求。
4. 对同一条视频至少间隔一段时间采两次公开播放量，验证增量、失败率和页面变化影响。
5. 记录采集方式、页面字段、请求频率、失败样例和来源时间。不要把抓取失败解释为播放量为零。
6. 核对 YouTube 当前服务条款、公开页面访问规则与可接受的抓取频率；若来源方式不稳定或不允许，先停止该适配器并提交替代方案审核。

阶段 0 通过条件：至少一个赛道的公开频道 Feed 与播放量快照能重复采集；名单和频道 ID 有来源记录；核心代码没有改动。

### 阶段 1：Premium 头部频道监控 MVP

首版从**全库约 20 个频道**开始，先覆盖科技 / AI、商业等用户明确需要的赛道；运行稳定、用户确实打开后再扩展到最多约 50 个。这里的“头部”代表经过人工核对的频道名单，不宣称是 YouTube 官方排名。

一期功能：

- 管理员维护赛道、频道 ID、显示名称、来源说明、启停状态和名单核验时间；无需终端用户手工填写频道 ID。
- Premium 用户在雷达面板勾选赛道及监控项，可开启或关闭头部频道新视频提醒。
- 服务端每 30 分钟轮询一次启用频道的新视频 Feed。按频道全局采集一次，再把新条目分配到选择相应赛道的 Premium 用户，禁止按用户数重复爬取。
- 对新视频记录标题、频道、发布时间、链接、封面、首次发现时间和最后检查时间。
- 对新发布视频做有限次数的播放量采样，例如发布后 1、3、6、24 小时附近；采样频率和保留期限应配置化，阶段 0 后按实际成功率和 VPS 资源调整。
- 先展示“最新发布”和可核验的播放量变化，不自动转录、不自动调用大模型、不自动生成分镜。
- 用户点击「用 HotCopy 整理」后，将该视频链接送入现有 HotCopy 处理流程。

### 阶段 2：已监控频道的流量异动

先只识别名单中频道的新视频表现，不扩展到全站黑马搜索。

每条雷达卡片展示可复核依据，例如：

- 最近一次采集时间及两次采样的播放量。
- 最近 1 小时或 3 小时的播放量增量与时速。
- 与该频道近期视频在相近发布时长下的中位播放速度相比，当前视频约为多少倍。
- 数据样本数不足、采样失败或基线缺失时显示“数据不足”，不显示爆发结论。

建议用时速和频道自身历史基线，而非单次总播放量除以频道粉丝数。初始阈值（例如达到近期基线的 3 倍）只作为可调参数，通过历史样本校准；不能把演示公式写成已验证的预测能力。

### 阶段 3：限定来源的黑马候选发现

“黑马”发现范围必须明确，不能声称监控整个 YouTube。先选有限候选来源，例如少数公开分类页、公开搜索结果页或管理员审核过的候选频道；具体来源先由阶段 0 证明可持续抓取。

- 为每个赛道限制每轮候选数和每日候选数；先抓轻量页面信息，再对候选视频抽样播放量。
- 黑马结论比较赛道内相似发布时长的视频增长分布。新频道缺少历史基线时标成“低置信度候选”，并展示实际样本。
- 增长异常先进入候选区，由用户或管理员复核；通过复核后再进入正式雷达收件箱。
- 不针对新博主自动批量拉音频、字幕或生成文案。

## 6. Premium 权限与同一应用体验

- HotCopy 主页面导航增加「爆款雷达」，界面沿用现有 HotCopy 样式与手机端布局。
- 已登录 Premium 用户可以查看和保存赛道配置、收件箱及异动卡片。
- 非 Premium 用户点击入口时显示现有会员升级面板，不返回雷达数据。
- 权限必须由雷达服务端验证，不能只依赖前端隐藏按钮。
- 新服务收到现有登录 Token 后，服务端调用 HotCopy 的 `/api/auth/me` 核验账号状态和 `plan`，不复制、共享或硬编码 `JWT_SECRET`。
- 现有 `/api/auth/me` 没有返回数字用户 ID。为避免读取核心 SQLite 或改动核心接口，雷达服务可以使用本地 `RADAR_ID_PEPPER` 对规范化邮箱做 HMAC，生成雷达内部用户键；数据库不保存原始 Token。密钥仅由服务器环境配置，绝不进入前端。
- 用户关闭某赛道后停止接收新条目；历史收件箱按产品确定的保留期保存，用户清空或删除时只影响雷达库。

## 7. 建议的数据结构（只建在附加服务 SQLite）

字段可在审核后调整，以下是最小可用结构：

### `radar_creators`

- `channel_id` 主键
- `category`、`channel_name`、`channel_url`
- `curated_rank`、`is_active`、`verified_source`
- `last_checked_at`、`last_success_at`、`last_error_code`

### `radar_user_settings`

- `user_key` 主键（服务器端 HMAC 标识）
- `categories_json`
- `track_top_creators`、`track_spikes`
- `updated_at`

### `radar_videos`

- `video_id` 主键
- `channel_id`、`category`、`title`、`video_url`、`thumbnail_url`
- `published_at`、`first_seen_at`、`last_checked_at`
- `latest_views`、`latest_views_at`
- `source_status`、`source_name`

### `radar_view_snapshots`

- `id` 主键
- `video_id`、`observed_at`、`views_count`
- 唯一约束 `(video_id, observed_at)`；建立 `(video_id, observed_at)` 查询索引。

### `radar_inbox`

- `id` 主键
- `user_key`、`video_id`、`reason_code`、`reason_detail`
- `is_read`、`created_at`
- 唯一约束 `(user_key, video_id)`，避免同一视频反复推送给同一用户，同时允许多个用户各自收到。

### `asset_usage`

- `user_key`、`billing_month`、`asset_type`、`used_count`、`updated_at`
- 唯一约束 `(user_key, billing_month, asset_type)`，资产生成前在事务中检查套餐 entitlement 并扣减；失败/超时的退款规则需明确，防止重试重复扣费。

### `asset_library`

- `id`、`user_key`、`asset_type`、`platform`、`title`、`content_json`、`created_at`、`updated_at`
- 只在用户点击保存后写入；保存成稿，不默认保存完整原字幕、媒体文件或模型内部推理内容。需提供用户维度的数据隔离、分页和删除。

不得把附加服务记录写入核心 `trends` 或 `copies_cache` 表。附加库只保留必要的公开元数据、播放量快照、资产额度和用户主动保存的成稿；不保存抓下来的完整视频或完整音频。

## 8. 服务边界与数据流

建议新增文件：

```text
backend/addons/
  server.js             # 附加 API：内容资产、雷达读取与权限校验
  worker.js             # 雷达采集任务，独立于 API 进程
  assets/               # 小红书图文、平台口播、知识脑图
  radar/                # YouTube 来源适配器与异动评分
  db.js                 # 附加服务 SQLite 初始化与参数化查询
  README.md             # 独立启动、日志、停止与回滚说明
```

数据流：

```text
HotCopy 页面
  → 附加 API（带现有 Bearer Token）
  → 核验 HotCopy /api/auth/me 与 Premium 状态
  → 读写附加服务 SQLite（雷达及资产表）

定时 Worker
  → 每轮按频道抓一次新视频 Feed
  → 对新视频及限定候选做公开播放量采样
  → 去重入库，按用户赛道设置写入收件箱

用户点击视频
  → 进入 HotCopy 现有 URL 输入/转换流程
```

## 9. 资源与稳定性控制

- 不运行无头浏览器，不下载所有候选视频，不在轮询任务里跑 Whisper 或 LLM。
- 轮询以公开 Feed 和小型页面请求为主；请求超时、退避和并发上限必须设置，首轮并发建议不超过 2。
- 每轮使用锁避免重复执行；任务崩溃后下轮可继续，重复抓取通过 `video_id` 幂等去重。
- 失败时保留最后一次有效数据，单独记录错误和最后成功时间；不得清空收件箱或将错误采集覆盖成零值。
- 附加 API 与雷达 Worker 由 PM2 独立于核心服务管理；明确独立日志、健康检查、重启策略和停止命令。先记录现有 VPS 内存 / CPU 基线，再做小名单试跑；只有实测资源余量足够才扩大频道数。资产生成应限制并发，避免多个 Pro/Premium 请求挤占 1G VPS 的内存。
- 如果雷达服务停机，核心 HotCopy 的健康检查、生成和支付回调不应受影响。

## 10. 初版 API 草案

所有接口只由附加服务提供，不新增到核心 `backend/server.js`：

- `GET /api/radar/overview`：套餐权限、已选赛道、各来源同步状态。
- `GET /api/radar/inbox?cursor=...`：游标分页返回当前 Premium 用户收件箱。
- `PUT /api/radar/settings`：保存赛道和监控开关；服务端校验枚举和数量上限。
- `PATCH /api/radar/inbox/:videoId/read`：标记已读。
- `DELETE /api/radar/inbox/:videoId`：从该用户收件箱移除。
- `POST /api/assets/xiaohongshu`：生成小红书图文；Pro/Premium 权限与独立资产额度由服务端校验。
- `POST /api/assets/short-video`：传入允许的平台枚举并生成分段口播脚本；Pro/Premium 权限与额度由服务端校验。
- `POST /api/assets/mindmap`：生成结构化脑图数据；Pro/Premium 权限与额度由服务端校验。
- `GET /api/assets/usage`：返回当前用户各类资产的月度已用量、上限与重置时间。
- `POST /api/assets/library`、`GET /api/assets/library`、`DELETE /api/assets/library/:id`：用户主动保存、查看和删除生成资产。
- `GET /health`：雷达进程健康检查，不返回密钥、用户邮箱或数据库内容。

雷达 API 响应应包含 `observed_at`、`source_status`、`last_success_at` 等时间和来源字段，前端按真实采集状态显示“最新”或“延迟”。资产 API 应返回 `asset_type`、`platform`、结构化 `content`、来源引用和额度状态；拒绝请求时返回明确的套餐升级或额度用尽状态，不在响应中暴露提示词、密钥或内部错误。

## 11. 验收标准

### 代码边界

- `backend/server.js` 中核心生成、转录、缓存、计费和模型代码无差异。
- 核心 `database.sqlite` 无新增附加服务表；雷达、资产库和资产额度只写入附加服务 SQLite。
- 现有 HotCopy 请求在雷达进程停止时仍可正常工作。

### 数据与权限

- 同一视频多轮抓取只生成一条 `radar_videos` 记录。
- 同一视频可进入多个符合赛道设置用户的收件箱，但每人最多一条。
- 非 Premium 用户不能通过直接请求雷达 API 读取或修改数据。
- Basic 用户继续得到当前已售核心权益；未获得资产 entitlement 的用户不能通过直接请求资产 API 绕过 Pro/Premium 限制。
- 资产服务额度与核心使用次数分别核算，返回值和页面标签清楚标示，不重复扣减或伪称共用额度。
- Token 不写入雷达数据库、URL、应用日志或错误回显。
- 采集失败保留原有统计值并明确显示失败/延迟。

### 体验

- 用户在现有 HotCopy 页面找到雷达入口，无需注册第二个账号或打开第二个产品。
- 手机窄屏可筛选赛道、浏览卡片、查看数据来源与时间，并一键进入现有 HotCopy 处理流程。
- 头部频道卡片显示真实视频标题、频道、链接、发布时间、实际采样播放量与采样时间。
- 黑马候选不显示保证性措辞；基线不足时明确显示样本不足。
- 三种新增资产均能在同一结果页生成、编辑、复制并按对应格式导出；手机窄屏下按钮、内容编辑和导出可操作。
- 生成文本保留原素材可追溯事实；脑图结构校验通过，非法/超长 Mermaid 内容不会被当作任意 HTML 执行。
- 未点击保存前不把完整原字幕写入长期资产库；用户保存后可在同一 HotCopy 账号下查看和删除。

### 稳定性

- 小名单连续试跑至少 7 天，记录每轮成功率、请求数、重复率、CPU、RSS 内存和失败原因。
- 试跑期间不出现核心进程重启、核心 API 错误率上升或磁盘持续增长失控。
- 未达到资源和抓取稳定性指标前，不扩至 50 个频道，不启用大范围黑马发现。

## 12. 交付顺序与审核门槛

1. **Antigravity 先审计划**：重点审核心冻结边界、采集来源可行性、Premium 鉴权、1G VPS 资源、YouTube 条款风险。
2. **代码盘点**：核对生产 PM2 启动方式、反向代理配置、API 域名、现有 Premium `plan` 和到期字段；不读取或输出 `.env`。
3. **套餐与成本审核**：对照当前 Basic/Pro/Premium 文案确认新增资产矩阵、每档月额度和每次生成的模型成本；保持现有售卖承诺，不做未公告的权益降级。
4. **资产生成 MVP**：先开发附加服务资产 API 与结果页三个入口；从现有原字幕生成小红书图文、平台定向口播、结构化脑图；完成额度台账、编辑/复制/导出、保存开关和手机端验收。该阶段不改核心文件。
5. **阶段 0 来源验证**：只增加附加服务中的独立雷达目录和临时验证脚本；不改核心文件，不部署生产。
6. **阶段 1 雷达 MVP**：来源验证通过后，开发频道新视频收件箱与站内 Premium 配置。
7. **小名单试跑**：用约 20 个已核实频道试跑至少 7 天，评估数据完整性、时效、资源以及资产生成并发对 VPS 的影响。
8. **阶段 2/3**：结果可靠后加入已监控频道的视频增长异动；黑马来源与范围另审，再逐步开放。
9. **发布**：Antigravity 审核和本地验收完成后，再单独审批 VPS / PM2 / 反向代理变更。本文本身不授权部署、推送或修改生产环境。

## 13. 请 Antigravity 审核并回答

1. 方案是否能在不修改 `backend/server.js` 核心转换逻辑的情况下落地？请列出任何必须碰核心文件的理由，并先提出替代方案。
2. 公开 Feed 与当前部署环境中的网页元数据解析，是否能稳定取得新视频和播放量？请用已验证频道实测，不要把可行性猜测写成已完成事实。
3. 对 20 个频道每 30 分钟发现新视频、对新视频做有限播放量快照，预计请求量、内存和磁盘增长是多少？测量结果是什么？
4. 同一用户身份如何在不共享 `JWT_SECRET`、不读取核心数据库、不改核心 `/api/auth/me` 的前提下验证？当前接口返回邮箱与套餐，可采用服务端 HMAC 用户键；请检查其生命周期和邮箱变更处理。
5. 新 API 域名 / 反向代理、CORS 和 Premium 直接请求防护是否完整？
6. 黑马候选源、阈值、样本不足和误报如何在界面上如实表达？
7. 若抓取失败、页面结构改变、VPS 内存紧张或雷达服务崩溃，如何单独停止和回滚，且不影响 HotCopy 核心服务？
8. 新增三个资产入口是否准确区分现有五平台口播改写与“按平台定向的结构化成稿”，避免重复宣传或修改旧权益？
9. 免费版“仅原字幕”与当前 Free/Basic 页面和服务端行为有何冲突？应如何处理存量套餐承诺，再决定是否调整套餐文案？
10. Pro 和 Premium 的资产额度各设多少才能覆盖模型成本并支撑 `$9.9 / $19.9` 定价？请给出按月成本测算，额度仅在审核后确定。
11. 附加服务月度用量账本、资产保存/删除、脑图安全渲染和手机端生成体验是否完整？

## 14. 已知限制

- 频道名单属于人工维护的精选池，不是客观、永久稳定的 YouTube 全球官方 Top 排名。
- 当前核心中的 `yt-dlp` 调用只负责媒体提取；雷达能否复用它读取播放量仍需阶段 0 验证。
- 一次页面抓取只能得出某个时间点的播放量；播放速度和“暴涨”必须依赖多次快照。
- YouTube `subscriberCount` 即使通过 Data API 获取，也会向下取到三位有效数字，因此不应单独作为精确黑马判定依据。[官方频道统计字段说明](https://developers.google.com/youtube/v3/docs/channels)
- 黑马发现只能覆盖已经定义并验证的候选来源，不能承诺扫描并发现全站所有新博主。
- 雷达发现后自动下载媒体、批量转录或自动替用户生成内容不在首版范围；用户主动点击内容资产入口时，才基于 HotCopy 已取得的原始字幕生成资产。
- 新内容资产调用附加服务的模型与额度账本，不复用或改写核心 `POST /api/generate` 的模型降级链及计数；模型成本和档位额度尚待审核。
